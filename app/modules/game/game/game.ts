
module app.game{

    export class GameExecutionController{

        private ships: Array;
        private ships: Array;

        constructor() {}

        start = function(ships: Array, cells : Array) {
            this.ships = ships;
            this.cells = cells;
        }
    }
}